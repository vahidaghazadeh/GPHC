name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24.x'
        
    - name: Build for Linux
      run: |
        VERSION="${GITHUB_REF_NAME#v}"
        GOOS=linux GOARCH=amd64 go build -ldflags="-s -w -X main.version=${VERSION}" -o gphc-linux-amd64 ./cmd/gphc
        
    - name: Build for macOS
      run: |
        VERSION="${GITHUB_REF_NAME#v}"
        GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w -X main.version=${VERSION}" -o gphc-darwin-amd64 ./cmd/gphc
        GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w -X main.version=${VERSION}" -o gphc-darwin-arm64 ./cmd/gphc
        
    - name: Build for Windows
      run: |
        VERSION="${GITHUB_REF_NAME#v}"
        GOOS=windows GOARCH=amd64 go build -ldflags="-s -w -X main.version=${VERSION}" -o gphc-windows-amd64.exe ./cmd/gphc
        
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: |
          gphc-linux-amd64
          gphc-darwin-amd64
          gphc-darwin-arm64
          gphc-windows-amd64.exe
        generate_release_notes: true
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
